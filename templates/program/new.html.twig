{% extends "base.html.twig" %}


{% block body %}
<main class="container">
    <div class="form mb-3">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="mt-3">Ajouter une série</button>
        {{ form_end(form) }}
    </div>
</main>
{% endblock %}