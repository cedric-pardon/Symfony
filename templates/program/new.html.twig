{% extends "base.html.twig" %}

{% block body %}
<main class="container">
    <div class="form mb-3 text-dark">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="mt-3 text-dark">Ajouter une série</button>
        {{ form_end(form) }}
    </div>
</main>
{% endblock %}