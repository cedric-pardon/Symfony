{% extends 'base.html.twig' %}

{% block title %}Acteurs{% endblock %}

{% block body %}
<h1 class="text-center">Liste des series de l'acteur {{ actor.name }} : </h1>
<main class="container">
    <div class="row justify-content-center gap-5">
        {% for program in actor.programs %}
        <div class="card bg-light mb-3 mb-5 p-0">
            <div class="card">
                <div class="card-body">
                    <h2 class="card-title">{{ loop.index }} - {{ actor.name }}</h2>
                    <p class="card-title">Série - {{ program.title }}</p>
                    <a class="btn btn-secondary p-2 px-4" href="{{ path('program_show', { slug: program.slug }) }}">{{ program.title }}</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    {{ include('actor/_delete_form.html.twig') }}
</main>
{% endblock %}